---
change_id: kebab-case-id
status: propose
created: YYYY-MM-DD
mode: supplement
---

### 单测 Spec — 需求名称

#### 0. 测试原则

* **Red/Green TDD** ：测试必须先 Red 再 Green，跳过 Red 的测试无法证明有效
* **First Run the Tests** ：开始前先跑已有测试套件，了解框架和基线
* **展示工作** ：必须展示 `go test -v` 实际输出，禁止"测试通过"等无证据声明
* **允许例外但必须记录** ：对历史系统、集成链路或难以稳定制造 Red 的场景，可退化为回归测试，但必须在本文档写明原因
* **验证等级优先** ：测试计划必须覆盖 `spec.md` 中声明的最低验证等级
* **不替代 `cc-apply`** ：`cc-test` 默认负责补强额外证据，不得把本应在 `cc-apply` 完成的最低验证事后补做成默认路径；只有 `recovery` 模式可基于 `blocked` / `partial` 记录补齐最低验证缺口

#### 1. 测试框架


| 项目              | 值                             |
| ----------------- | ------------------------------ |
| 测试执行器        | Go native testing (`go test`)  |
| 增强断言/Mock框架 | Testify (assert/mock) / GoMock |
| 已有测试数量      |                                |
| 已有测试风格      |                                |

#### 1.1 测试层级选择

| 项目 | 值 |
|------|----|
| 主测试层级 | `unit / repo / transport / chain / integration / manual` |
| 选择原因 | |
| `cc-test` 模式 | `supplement / recovery` |
| recovery 对应阻塞记录 | |
| 更高层测试是否跳过 | 是/否 |
| 跳过原因 | |
| bugfix 回归路径 | |

#### 1.2 已完成的最小验证

记录哪些 task 已在 `cc-apply` 中完成最小回归验证，以及当前仍未覆盖的风险。

| Task | 涉及映射项 | `cc-apply` 内已完成验证 | 证据 | 尚未覆盖的风险 |
|------|------------|-------------------------|------|----------------|

#### 1.3 验证差距与补强计划

这里默认只需要把 `spec.md` 中“需求-验证映射”里尚未闭合的项补齐，不必重新罗列一整套无关测试计划。
优先沿用 `spec.md` 中的映射编号，不重新发明另一套编号体系。

| 映射编号 | 需求项 / 风险点 | 当前闭环状态 | `cc-apply` 已有证据 | 本次需补齐证据 | 跳过原因 | 替代证据 | 剩余风险 |
|----------|------------------|--------------|----------------------|----------------|----------|----------|----------|
| V1 | | todo / apply-covered / test-covered / gap | | | | | |

#### 2. 覆盖范围

仅在变更较复杂、风险较高或需要系统性补强说明时展开；简单需求可只保留 P0/P1 的核心条目。

##### P0 — 核心业务逻辑（必须覆盖）

###### 结构体: UserService


| 方法 | 场景 | 输入 | Mock 行为 | 预期结果 |
| ---- | ---- | ---- | --------- | -------- |

##### P1 — 数据访问层

##### P2 — 入口层/服务层

#### 2.1 分层覆盖说明

仅当确实存在“为什么跳过更高层验证”的解释成本时填写；简单变更可写 `按 1.1 与 1.3 执行，无额外展开`。

| 层级 | 是否覆盖 | 覆盖对象 | 说明 |
|------|----------|----------|------|
| `unit` | 是/否 | | |
| `repo` | 是/否 | | |
| `transport` | 是/否 | | |
| `chain` | 是/否 | | |
| `integration` | 是/否 | | |
| `manual` | 是/否 | | |

##### 不测试（明确列出原因）

#### Red/Green 例外说明

只有存在 Red 难以稳定构造、只能退化为回归验证的场景时才填写；否则可写 `无`。

| 场景 | 原因 | 替代验证方式 |
|------|------|--------------|
| 历史系统缺少稳定隔离点 | 无法先构造可重复 Red | 补充回归测试并记录局限 |
| 集成依赖不可控 | 失败前置条件无法稳定制造 | 使用回归路径或 mock 证明 |
| 缺陷修复无法前置重现 | 只能通过修复后回归验证 | 记录原因并展示回归证据 |

#### 2.2 补强验证目标

只有 `cc-test` 需要额外补齐高层证据或发布前安全证据时才展开；否则可写 `无额外补强`。

说明本次 `cc-test` 需要额外补齐的验证、其必要性，以及若跳过更高层验证所采用的替代证据。

| 项目 | 值 |
|------|----|
| `cc-test` 需补强或 recovery 补齐的验证 | |
| 补强原因 | |
| 跳过的更高层测试 | |
| 跳过原因 | |
| 替代证据 | |

#### 3. 执行计划

默认只保留本次真正会执行的步骤；不需要为了模板把所有层级都走一遍。

* [ ]  Step 1: 运行已有测试套件 (`go test ./...`)，确认基线
* [ ]  Step 2: 生成 P0 测试 → 确认 Red → 确认 Green
* [ ]  Step 3: 生成 P1/P2 测试
* [ ]  Step 4: 补强 `spec.md` 对应的链路/集成/手工验证证据；若为 `recovery`，补齐已记录阻塞的最低验证缺口
* [ ]  Step 5: 运行完整测试套件，确认覆盖率 (`go test -cover`)
